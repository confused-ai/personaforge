# Agents

This mirrored guide now defers to the primary docs tree for executable examples.

- Start with the primary agents guide and examples before adding advanced runtime features.
- Use the single `personaforge` package instead of internal package names.
- Prefer `agent()`, `createAgent()`, or `bare()` based on the level of control you need.