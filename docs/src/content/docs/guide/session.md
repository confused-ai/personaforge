# Session

This mirrored guide now points to the primary session guide.

- Use the main docs tree for current session stores and continuity patterns.
- Keep session imports on `personaforge/session`.
- Use the primary guide when you need a runnable example.