# Database

This mirrored guide now points to the primary database guide.

- Use the main docs tree for current database integration patterns.
- Keep database access behind tools, adapters, or storage abstractions as needed.
- Avoid internal package imports; stay on the public `personaforge` surface.