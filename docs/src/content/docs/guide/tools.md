# Tools

This mirrored guide now points to the primary tools guide.

- Use the main docs tree for current `tool()` and `personaforge/tool` usage.
- Keep mirrored content prose-only so duplicate snippets do not drift.
- Treat the primary guide as the checked source of truth.