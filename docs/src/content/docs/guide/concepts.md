# Concepts

This mirrored page now stays prose-first so it remains accurate.

- Think in layers: agent, tools, session, runtime, orchestration, and production controls.
- The public story is one package, `personaforge`, with optional subpaths.
- Use the primary docs tree for runnable snippets and implementation details.