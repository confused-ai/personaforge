# Background Queues

This mirrored guide now points to the primary queue documentation.

- Use the main guide for current queue integrations and lifecycle behavior.
- Keep queue setup aligned to the single-package `personaforge` API.
- Add background processing only after the foreground agent path is stable.