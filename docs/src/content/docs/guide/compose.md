# Compose

This mirrored guide now points to the primary compose documentation.

- Use the main guide for the checked `compose()` and `pipe()` workflow examples.
- Keep composition code in the root `personaforge` API unless a module-specific subpath is required.
- Reach for orchestration only when fixed pipelines are no longer enough.