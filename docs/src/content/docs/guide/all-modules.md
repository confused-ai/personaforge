# All Modules

This mirrored page summarizes the current package story.

- Install one package: `personaforge`.
- Use subpaths only for focused modules such as serve, session, observe, orchestration, reasoning, scheduler, tool, adapters, and guard.
- Treat the primary docs tree as the source of truth for runnable examples.