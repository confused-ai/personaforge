# Workflows

This mirrored guide now points to the primary workflows guide.

- Use the main docs tree for current workflow, compose, and orchestration patterns.
- Keep imports on the public `personaforge` surface.
- Prefer the checked primary snippets when building real flows.