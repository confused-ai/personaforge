# Custom Tools

This mirrored guide now points to the primary custom tools guide.

- Use the main guide for checked `tool()` examples and composition helpers.
- Prefer source-backed examples over duplicated snippets in the mirrored tree.
- Keep public imports on `personaforge` or `personaforge/tool`.